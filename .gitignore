__pycache__/
*.py[cod]
.cache/
*.egg-info/
.venv/
venv/
.DS_Store

# Credentials never belong in this repo (it is public). ESPN cookies live in
# ~/.config/draftkit/secrets.json instead; these are belt-and-braces.
secrets.json
*secret*
*cookie*
.env
